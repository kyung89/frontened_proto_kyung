import React from "react";

// 설계안 필요
// 로그인 멤버의 ROLE 에 따라 보여주는 페이지를 다르게 할 것: 일단은 작물선택 관리

export default function SettingsPage() {
  return (
    <div className="w-full h-full flex flex-col justify-center items-center">
      SettingsPage
    </div>
  );
}
